{
  "name": "forward-one",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "voice:generate": "node scripts/generate-guide-voice.mjs"
  },
  "devDependencies": {
    "kokoro-js": "^1.2.1",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
